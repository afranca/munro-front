import React from 'react';
import './Filter.css';
import FilterForm from './FilterForm';

 const Filter = (props) => {

 
    return (
        <div className="filter">
          <FilterForm  />
        </div>
      );

 }
 export default Filter;
